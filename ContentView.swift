//
//  ContentView.swift
//  Number
//
//  Created by 恩恩 on 2021/8/6.
//

import SwiftUI

struct Level16Screen: View {
    
    @Binding var gameMode: GameMode
    @Binding var levelProgress: Int
    @Binding var currentLevel: Int
    @Binding var detectOn: Bool
    @Binding var passLevel: Bool
    
    var body: some View {
        Text("123")
    }
}

struct Level17Screen: View {
    
    @Binding var gameMode: GameMode
    @Binding var levelProgress: Int
    @Binding var currentLevel: Int
    @Binding var detectOn: Bool
    @Binding var passLevel: Bool
    
    var body: some View {
        Text("123")
    }
}

struct Level18Screen: View {
    
    @Binding var gameMode: GameMode
    @Binding var levelProgress: Int
    @Binding var currentLevel: Int
    @Binding var detectOn: Bool
    @Binding var passLevel: Bool
    
    var body: some View {
        Text("123")
    }
}

struct Level19Screen: View {
    
    @Binding var gameMode: GameMode
    @Binding var levelProgress: Int
    @Binding var currentLevel: Int
    @Binding var detectOn: Bool
    @Binding var passLevel: Bool
    
    var body: some View {
        Text("123")
    }
}

struct ContentView_Previews: PreviewProvider {
    
    
    static var previews: some View {
        MainScreen()
    }
}
